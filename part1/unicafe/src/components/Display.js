const Display = (props) => {
    if (props.total === 0) {
        return(
            <div>
                There is no feedback given
            </div>
        )
    }

    return (
        <div>

        </div>
    )
}

export default Display
